# Employee Management System

A simple command-line Employee Management System built with Python and MySQL.

## Features
- Add employee
- List employees
- Get employee by ID
- Update employee
- Delete employee

## Setup
1. Clone the repository
2. Create a virtual environment
3. Install dependencies
4. Create a `config.py` file with MySQL credentials
5. Run `python main.py`